<UserControl x:Class="BlueDwarf.Controls.UrlBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignWidth="400"
             >
    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ComboBox Grid.Column="0" x:Name="SchemeComboxBox" SelectionChanged="OnSchemeChanged" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="0" x:Name="SchemeTextBlock" VerticalAlignment="Center"/>
        <TextBlock Grid.Column="1" Text="://" VerticalAlignment="Center"/>
        <TextBox Grid.Column="2" x:Name="HostTextBox" TextChanged="OnHostChanged" VerticalAlignment="Center" />
        <TextBlock Grid.Column="3" Text=":" VerticalAlignment="Center"/>
        <TextBox Grid.Column="4" x:Name="PortTextBox" TextChanged="OnPortChanged" VerticalAlignment="Center" Width="50"/>
    </Grid>
</UserControl>
