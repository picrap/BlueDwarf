<UserControl x:Class="BlueDwarf.Controls.Loading"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" x:Name="This">
    <Border Background="#C0FFFFFF" Visibility="{Binding Show, ElementName=This, Converter={StaticResource BoolToVisibilityConverter}}">
        <ProgressBar VerticalAlignment="Center" Height="12" Margin="16" IsIndeterminate="True" />
    </Border>
</UserControl>
