<mui:ModernWindow x:Class="BlueDwarf.View.HomeView"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                  xmlns:mui="http://firstfloorsoftware.com/ModernUI"
                  xmlns:viewModel="clr-namespace:BlueDwarf.ViewModel"
                  xmlns:controls="clr-namespace:BlueDwarf.Controls"
                  mc:Ignorable="d" 
                  d:DesignHeight="300" d:DesignWidth="300"
                  Title="Blue Dwarf" IsTitleVisible="True"
                  d:DataContext="{d:DesignInstance Type=viewModel:ConfigurationViewModel, IsDesignTimeCreatable=true}" WindowStyle="ToolWindow"
                  ContentSource="/View/TunnelConfigurationView.xaml" ResizeMode="NoResize"  >
    <mui:ModernWindow.MenuLinkGroups>
        <mui:LinkGroup DisplayName="Configuration">
            <mui:LinkGroup.Links>
                <mui:Link DisplayName="Tunnel" Source="/View/TunnelConfigurationView.xaml" />
                <mui:Link DisplayName="Proxy" Source="/View/ProxyConfigurationView.xaml" />
                <mui:Link DisplayName="Keepalive" Source="/View/LocalProxyKeepaliveView.xaml" />
            </mui:LinkGroup.Links>
        </mui:LinkGroup>
        <mui:LinkGroup DisplayName="Status">
            <mui:LinkGroup.Links>
                <mui:Link DisplayName="Keepalive" Source="/View/StatusView.xaml" />
            </mui:LinkGroup.Links>
        </mui:LinkGroup>
    </mui:ModernWindow.MenuLinkGroups>

    <Grid>
        <controls:CloseButton Command="{controls:Command {Binding Minimize}}" />
        <controls:Hider Show="{Binding Show, Mode=OneWay}" />
    </Grid>
</mui:ModernWindow>
